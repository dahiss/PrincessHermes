{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/init /opt/hermes/docker/main-wrapper.sh gateway run --replace",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 10
  }
}
